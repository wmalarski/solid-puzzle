import { type Component } from "solid-js";

export const RectanglesGroup: Component = () => {
  // createEffect(() => {
  //   if (props.tool === "selector") {
  //     useDeselect();
  //   } else if (props.tool === "creator") {
  //     useCreator();
  //   }
  // });

  return null;
  // return (
  //   <For each={props.samples}>
  //     {(sample) => <Rectangle sample={sample} tool={props.tool} />}
  //   </For>
  // );
};
